# Gradle daemon JVM (incubating). Must match a locally installed JDK 17; not auto-provisioned.
# Omit vendor so Temurin, Homebrew, Android Studio jbr, etc. can satisfy the criteria.
toolchainVersion=17
