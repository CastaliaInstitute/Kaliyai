# Copy to `.env` (gitignored) or rely on sibling repo:
#   ../castalia.institute/.env (GEMINI_API_KEY is read by Gradle if this file is empty/absent locally).
# Rebuild the app after changing.
# GEMINI_API_KEY=your_key_here
GEMINI_API_KEY=
